1. Create git repo and clone it to local
2.create mycode.py and add code to it. it will save a csv file to a new file "data" folder
3.Do a git add-commit-push before initialising dvc
#pip install dvc
4. Now we we do dvc init(creates .dvcignore, .dvc)
5. create a fake s3 folder 
6. now we do "dvc remote add -d myremote S3"
       in real scenario we paste aws s3 url instead of s3
7.Next "dvc add data/"
        tell dvc to track everything in the folder
        if you want specific files tracking give data/filename
8. You get an error cuz git also tracking.
        use This command "git rm -r --cached 'data'"
        and commit using "git commit -m "stop tracking data""
        data.dvc file show the tracking ids 
        data/ goes to gitignore
9. again we do dvc add and git add.
         dvc add data/
         git add .gitignore data.dvc
10. git status and dvc status

11.git log --oneline

12.make a gitadd and  commit "DVC initiated with data versioning"

    also push git

13. so far dvc is tracking but not puting destination which is s3

14. first commit and push dvc
        "dvc commit"

15. now again git add-commit-push

16. now make changes to mycode.py